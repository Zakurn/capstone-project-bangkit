<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.HeloTani" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/btn_login</item>
    </style>

    <style name="Theme.HeloTani.Splash" parent="Theme.HeloTani">
        <!-- Set background warna splash screen -->
        <item name="android:windowBackground">@color/white</item>
        <!-- Tema ini akan digunakan setelah splash screen selesai -->
    </style>

</resources>